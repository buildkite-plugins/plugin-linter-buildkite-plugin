steps:
  - label: ":sparkles: Lint"
    plugins:
      $BUILDKITE_REPO#$BUILDKITE_COMMIT:
        id: plugin-linter

  - label: ":bash: Shellcheck"
    plugins:
      shellcheck#v1.0.1:
        files: hooks/**

  - label: ":bash: Tests"
    plugins:
      docker-compose#v2.4.0:
        run: tests